# Web (Next.js)

Placeholder for the **Next.js App Router** frontend (SEO landing + dashboard), **shadcn/ui**, and Tailwind.

Scaffold here when ready, for example:

```bash
cd apps/web
npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir
```

The inference API lives in `services/inference-api/`; call it from Route Handlers or the browser per `.cursor/rules/frontend-apps.mdc`.
