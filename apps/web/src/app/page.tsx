import { StudioEditor } from "@/components/studio/studio-editor";

/** Image editor (Higgsfield-style bottom dock) — default app entry. */
export default function HomePage() {
  return <StudioEditor />;
}
