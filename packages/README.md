# Shared packages (optional)

Use this folder for workspace-local packages, for example:

- TypeScript types generated from the inference API OpenAPI schema
- Shared constants used by `apps/web` and future clients

Nothing is required here for the Python-only inference MVP.
